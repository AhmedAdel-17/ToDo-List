// ../src/CommonComponents/LogoutButton/LogoutButton.styles.ts
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    marginLeft: 15, // Adjusts the position in the header
    padding: 10,
  },
});

export default styles;
